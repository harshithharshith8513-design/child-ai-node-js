services:
  - type: web
    name: childguard-ai
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DJANGO_SECRET_KEY
        generateValue: true
